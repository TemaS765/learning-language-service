{% extends 'base.html.twig' %}

{% block body %}
    <div class="d-grid gap-2 col-6 mx-auto">
        <a class="btn btn-primary p-3 text-uppercase" href="{{ url('train') }}">Обучение</a>
        <a class="btn btn-primary p-3 text-uppercase" href="{{ url('exam') }}">Проверка</a>
        <a class="btn btn-primary p-3 text-uppercase" href="{{ url('add_word') }}">Добавить слова</a>
        <a class="btn btn-primary p-3 text-uppercase" href="{{ url('get_words') }}">Мои слова</a>
    </div>
{% endblock %}